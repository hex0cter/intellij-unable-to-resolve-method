package com.example.kirk.step

/**
 * Possible step execution types
 */
enum ExecutionType {
    /**
     * Default execution type:
     *  the step passes if no exceptions are thrown
     *  otherwise the step fails
     */
    EXECUTE
}
